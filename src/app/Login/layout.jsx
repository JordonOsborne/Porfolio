export const metadata = {
	title: 'JOsborne.dev | Login',
	description:
		'Login to view Client information and have personallized experience.',
};

export default function layout({ children }) {
	return children;
}
