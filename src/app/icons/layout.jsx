export const metadata = {
	title: 'JOsborne.dev | Icons',
	description:
		'Full searchable icons library used in JOsborne.dev porfolio website.',
}

export default function layout({ children }) {
	return children
}
